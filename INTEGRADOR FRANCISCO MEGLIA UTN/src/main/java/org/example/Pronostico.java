package org.example;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

public class Pronostico {
/*
        static int ganadorPronostico;

       public static void leerPronostico (int cantPartidos , String ruta) {

           try {
               BufferedReader reader = new BufferedReader(new FileReader(ruta));
               String linea = reader.readLine();
               int contadorLineas = 1;
               while (linea != null && contadorLineas <= cantPartidos) {

                   String[] lineaToArr = linea.split(";");
                   new Equipo(lineaToArr[0]);
                   new Equipo(lineaToArr[4]);
                   if (lineaToArr[1].equals("X")) {
                       ganadorPronostico = 1;
                   } else if (lineaToArr[3].equals("X")) {
                       ganadorPronostico = 2;
                   } else if (lineaToArr[2].equals("X")) {
                       ganadorPronostico = 0;
                   }
                   System.out.println("El resultado pronosticado del partido " + contadorLineas + " es: " + ganadorPronostico);
                   contadorLineas ++;
                   linea = reader.readLine();
               }
               reader.close();
           } catch (IOException e) {
               e.printStackTrace();
           }
       } */
}


